backend:
  image: com.monkeygroover/backend
  ports:
   - "9001:9001"
  expose:
   - "9001"
  environment:
   - HOST=127.0.0.1
   - PORT=9001
   - BIND_HOST=0.0.0.0
   - BIND_PORT=9001
   - LOG_LEVEL=DEBUG

